# CHANGELOG

## v1.0.245
- 开源与安全加固：`chat-proxy.js`、`db.js`、`init-db.js`、`server-prod.js` 中的硬编码模型 API Key、数据库密码与 JWT 密钥全部改为从环境变量注入，`JWT_SECRET` 缺失时服务拒绝启动，并补充 `.env.example` 说明
- 移除 LearnWords 与 AILearnWordsDemo 页面及路由，删除不再使用的 `@xyflow/react`、`d3-force`、`markdown-it` 依赖，清理 `useStore` 遗留聊天历史状态与 `db.js` 废弃接口
- MindmapChat 页面按职责拆分为 `src/pages/mindmap/` 下的输入、消息、建议、导图视图、类型与工具模块
- `package.json` 标记为公开并添加 MIT 许可，桌面端默认服务器地址改为环境变量配置

## v1.0.244
- 人格请求异常时先记录安全诊断日志，再以递增延迟和随机抖动内部重试最多 2 次；连续失败后按限流、超时、鉴权、网络或服务异常返回分类兜底，并移除前端“再唤醒一次”手动重试功能

## v1.0.243
- 人格分裂改为每个人格独立发起一个前后端请求并并发生成，所有人格完成后再通过独立接口生成综合总结；统一取消、单人格失败展示和重试能力保持不变

## v1.0.242
- 人格分裂的角色回答、失败重试和最终总结统一进入服务端全局限速队列，模型调用按单并发且至少间隔 1.2 秒启动，并支持排队取消和更长的整体超时以适配 QPS 限制

## v1.0.241
- 修复脑内总结过长时撑出嘴形容器和屏幕的问题；桌面端与手机版使用固定嘴形高度，问题和结果在各自区域内滚动展示

## v1.0.240
- 大脑完成态的脑内总结改为同时显示本轮实际发送的问题和最终综合结果；问题与结果使用独立标签和滚动区域，长内容不会撑高嘴形容器或页面，桌面端与手机版同步适配

## v1.0.239
- 一次性体验、随机选择 6 个、重新玩一次及不留存说明全部迁入左侧角色菜单，右侧桌面区域移除顶部工具栏并恢复为完整高度的纯效果舞台；手机版仅保留必要的人格菜单悬浮入口
- 随机选择 6 个改为完整重开操作：可在任意阶段终止当前请求，清空搜索、问题、回答、总结、错误、重试和球体布局后，再无重复选择 6 个角色

## v1.0.238
- 人格分裂为所有角色模板统一追加精简回答提示：使用 1—3 个短句、建议 30—60 字、最多 80 字，并禁止寒暄、自我介绍、问题复述和无关铺垫；服务端输出预算与硬性截断同步收紧
- 页面头部新增无重复“随机选择 6 个”能力，并将一次性体验、随机选择和重新玩一次统一收拢到左侧按钮组；手机版保持触控尺寸和横向可用性

## v1.0.237
- 手机版角色选择升级为带明确关闭入口和选择数量提示的全屏面板，顶部栏、大脑、嘴形输入及操作按钮按触控尺寸重新排布，并适配刘海屏和底部安全区
- 手机版人格结果由双列长卡片改为保持球形的横向滑动轨道，支持居中吸附、相邻球预览和单层页面滚动，恢复从大脑蹦出观点的视觉体验且减少页面高度

## v1.0.236
- 修复人格分裂嘴形输入区仍被 Ant Design 运行时样式渲染成白色矩形的问题，统一默认、悬停、聚焦和禁用状态为透明背景，使输入内容自然融入大脑底色

## v1.0.235
- 嘴形区域改为透明背景以延续大脑底色，嘴内只显示问题或脑内总结；发送、复制和分享按钮统一移到嘴部下方
- 人格侧栏移除类型、地区和性格三个下拉筛选及对应状态逻辑，保留角色搜索与选择能力

## v1.0.234
- 大脑视觉升级为更柔和的粉紫渐变、圆润脑回纹理、高光大眼睛与腮红表情，输入和完成态统一使用同一个嘴形容器
- 本轮完成后由主持人总结直接替换嘴内问题显示，原问题仍保留用于复制、分享和人格重试；移除独立总结卡片，长总结在嘴内滚动且不撑高页面
- 桌面端大脑底部改为紧贴页面底部，布局安全区同步使用 0px 底部偏移，保持球体与大脑的安全距离

## v1.0.233
- 桌面端人格球目标直径提升至 180—220px，并改为从目标尺寸开始尝试、优先利用大脑左右空间，仅在当前视口不足时逐级缩小
- 大脑最大高度提升至 360px，发送时按实际舞台与大脑尺寸计算布局；桌面内容固定在当前视口并预留总结区域，确保总结出现前后球体位置不变且不越出页面高度
- 增加常规、窄屏及较矮视口下的大球、高脑袋、边界与安全距离回归测试，移动端继续使用自适应卡片布局

## v1.0.232
- 人格球从固定双排锚点升级为带种子的二维随机松散分布，每轮布局不同且同一轮保持稳定，同时继续满足球体碰撞、舞台边界和大脑安全距离约束
- 大脑新增带眨眼和视线移动效果的双眼，输入框改为嘴形交互区域并在聚焦、思考时反馈；移动端同步适配且支持减少动态效果
- 桌面舞台改为视口自适应高度并压缩上下留白，在保持 2—6 个球安全布局的同时减少非必要页面滚动

## v1.0.231
- 人格球改为发送时一次性预分配的种子随机双层布局，同一轮逐个返回时位置保持稳定，重新开始后自动生成新的松散分布
- 增加球体碰撞、舞台边界与大脑安全区检测，保证桌面及窄屏桌面下 2—6 个球互不覆盖、球间至少 24px 且距大脑至少 50px；移动端继续使用双列卡片布局

## v1.0.230
- 新增一次性“人格分裂”体验页：支持从现有角色模板中筛选并选择 2—6 个人格，将同一问题并行发送并以脑形弹球动画逐个展示观点，完成后生成综合结论
- 人格回答由提示词和服务端双重限制为最多 100 个可见字符，支持单人格失败后重试、复制与分享；离开、刷新或重置即清空，不创建会话、面板、消息或本地缓存
- Web 与 MindmapChat Desktop 共用新入口和无状态 SSE 接口，补充取消、超时、部分失败处理及安全元数据日志

## v1.0.229
- 角色会话名称与根节点标题统一使用角色名称 `name`，角色描述 `title` 不再作为会话名称；已有角色会话下次进入时自动更名，并继续兼容旧描述标题的历史会话认领

## v1.0.228
- 角色模板改为每位用户唯一长期会话：首次点击创建、再次点击恢复原会话，数据库唯一约束与事务处理可避免网页、多窗口和桌面端并发重复创建
- 历史角色会话按模板特征安全认领内容最多且最近更新的一条，其余历史不删除；删除模板后仍保留会话，并补充创建/复用操作日志

## v1.0.227
- 新增 `mindmapchatdesktop` Electron 跨平台桌面客户端，支持持久登录、服务器地址设置、断线重试、外链安全跳转、单实例窗口和原生菜单
- 新增 macOS、Windows、Linux 打包配置与 MindmapChat 应用图标，并生成通过启动验证的 macOS ARM64 应用目录

## v1.0.226
- 完善发布健康检查：验证构建产物、远端进程与线上 HTTP 接口状态

## v1.0.225
- 部署流程改为 Git 提交推送成功后再发布，强制校验干净工作区与远端同步，并使用完整 TypeScript + Vite 生产构建
- 发布脚本严格传递失败状态并增加线上 HTTP 健康检查
- 统一项目自动化脚本的执行说明、相对路径与 `MindmapChat` UI 元数据，移除过期文件名与旧路径

## v1.0.224
- 将 MindMapChat 项目专用 Skill 迁移至仓库标准目录 `.agents/skills`，补充 UI 元数据并改为自动定位项目根目录
- 清除 Skill 中的明文凭据和用户绝对路径，改由环境变量或权限受限的用户私有配置文件提供部署与 Codeup 凭据

## v1.0.223
- 将浏览器标题、登录/注册页品牌标识、服务启动日志及 npm 包名统一为 `MindmapChat`，并保留旧数据库、登录存储键和部署目录以兼容现有数据

## v1.0.222
- 将用户级开发工作流 Skill 从 `skill-develop-workflow` 重命名为 `skill-mindmapchat-develop-workflow`

## v1.0.221
- 修复点击「思维导图」后因摘要状态/回调越作用域导致的整页白屏，增加应用级错误边界，并恢复 TypeScript 生产构建
- 聊天响应兼容 SSE 与非流式 JSON，401/异常可正确结束加载；会话切换增加竞态保护，持久化补齐分组、置顶、背景、摘要与图片字段
- 会话自动保存按会话隔离防抖，服务端全量同步改为数据库事务，避免跨会话取消保存及部分写入造成的数据损坏

## v1.0.220
- 将用户级开发工作流 Skill 从 `change-workflow` 重命名为 `skill-develop-workflow`

## v1.0.219
- 将用户级部署 Skill 从 `deploy-thinkchat` 重命名为 `skill-mindmapchat-deploy`，并同步更新关联调用与脚本路径

## v1.0.218
- 修复总结接口 500：callLLM 配置解析改为与正常对话一致（客户端配置 > 服务端全局配置 > 默认值），并传入管理员全局配置，避免依赖已过期的硬编码 key
- 总结接口增加服务端错误日志，失败时返回明确错误信息（不再泛化 500）

## v1.0.217
- 容器总结增加轻量反馈：总结开始/完成/失败时在浏览器控制台打印「[摘要] …」，并在页面底部居中显示不抢眼的通知条（小转圈 + 「「xx」节点自动生成摘要…/摘要已生成」），自动收起

## v1.0.216
- 容器节点对话总结：新增后端 `POST /api/panels/:id/summarize` 接口（非流式调用大模型），panels 表增加 `summary`/`summary_msg_count` 字段并透传至前端与同步接口
- 前端自动总结（后序 DFS，先子容器后父容器，父吸收子摘要）：定时轮询（5 分钟）+ 消息数阈值（≥40 条）双触发，跳过当前输入容器与已最新容器；发消息时上下文中离路径且已总结分支自动用摘要替代原始消息
- 节点头部增加「总结 / ✓已总结」标记（可手动触发、悬停看摘要）；总结中显示不可点击的半透明蒙层 + 转圈动画

## v1.0.215
- 对话消息支持 ```html 围栏代码块：消息完全输出完成后自动将块内 HTML 渲染为真实浏览器效果；流式输出中先显示为代码块，完成瞬间切换。右上角保留「源码」切换查看原始文本

## v1.0.214
- 对话消息含 HTML 标记块时直接渲染真实浏览器效果（移除仿浏览器窗口卡片与左上角「HTML」标记）；保留右上角「源码」切换按钮

## v1.0.213
- 对话消息 HTML 主内容改为仿浏览器窗口卡片渲染（红黄绿三点 + 「HTML 预览」标签 + 查看源码切换），呈现浏览器预览效果
- 文本输入框移除右下角原生 resize 手柄，改为顶部拖拽条调整高度（仅允许从上方调整）

## v1.0.212
- /mindmapchat 球体参数面板高度默认值调整为 2.00x
- 对话消息渲染（MarkdownRenderer）接入 rehype-raw：内容含 HTML 时显示「HTML」标记，支持「渲染预览 / 查看源码」切换

## v1.0.211
- /mindmapchat 球体参数面板新增「高度」调节（缩放 radiusY）；默认参数调整为倾斜度0 / 大小1.00x / 宽度0.95x / 高度1.00x，复制参数 JSON 同步增加 heightScale

## v1.0.210
- /mindmapchat 首页角色球体新增客户端可调参数面板（左下角「球体参数」）：支持实时调整倾斜度、节点大小、球体宽度，并可一键复制参数 JSON、重置默认值

## v1.0.209
- mindmapchat 主页 3D 球体铺满整个屏幕：改为椭圆球体（radiusX/radiusY 分别按视口宽高 48% 计算），节点尺寸和透视距离同步适配

## v1.0.208
- 手机端侧边栏改用 fixed 定位的自定义覆盖层实现（不依赖 Ant Design Drawer），确保可靠显示和关闭

## v1.0.207
- 手机端侧边栏改用 Drawer 覆盖层实现（点击遮罩可关闭），修复侧边栏无法关闭的问题

## v1.0.206
- 修复手机端拖拽星球时页面高度突然变窄的问题（visualViewport 误判为键盘弹出）

## v1.0.205
- /mindmapchat 工具条优化：“总结”按钮移至“多选”旁边并用紫色区分，“发送”按钮改为图标+文字组合

## v1.0.204
- /mindmapchat 将“总结”和“发送”按钮从输入框下方移至多选工具条右侧，布局更紧凑；欢迎页输入框下方同样添加总结+发送按钮

## v1.0.203
- 登录页面密码错误时显示可见的内联错误提示文案（红色警告框+输入框错误状态）
- /mindmapchat 手机端侧边栏默认关闭，左上角菜单按钮点击打开，可关闭
- /mindmapchat 手机端会话列表固定高度+滚动条，支持触摸滑动

## v1.0.202
- 英语学习智能体老师端重构为分步流程：选择单词（手动输入/上传图片）→ 选择作业模式（单词卡/翻译/拼写/填空）→ 生成单词卡预览 → 下发给学生 → 查看学生成绩 → 学情分析（高频错词 TOP5、各题型正确率）；新增动画 keyframes 注入，步骤指示器支持点击回退

## v1.0.201
- 英语学习智能体页面全面重构为三端交互演示：老师端（选择单词、生成多样化题目、一键下发、查看学生完成情况）、学生端（完成单词卡/翻译/拼写题目、实时反馈、错题本自动收录、学习报告）、家长端（本周学习统计、每日学习情况可视化图表）；采用 mock 数据，交互友好，流程完整

## v1.0.200
- 英语学习智能体演示页样式全面优化：简洁大气设计，去除重紫色渐变背景，改用清爽浅灰配色；新增设计令牌系统统一视觉风格；Hero 区域采用居中布局+装饰性径向渐变；数据卡片独立展示带入场动画；主流程增加教/学/评/管阶段分组标签和时间线布局；角色展示改为左右分栏设计；功能清单改为 3 列网格布局；添加微妙的入场动画和过渡效果

## v1.0.199
- 修复 CSS 滚动条样式警告（`:::-webkit-scrollbar` → `::-webkit-scrollbar`）

## v1.0.198
- 单词学习页面重新设计：去除 AI 痕迹，采用简洁大气的极简风格，去除多余 emoji 和装饰，黑白灰配色

## v1.0.197
- 移动端布局优化：左侧菜单始终可见（宽度180px），进入页面即可看到菜单+主页面；对话视图下菜单+输入框并排显示，思维导图不再占用移动端空间
- 移除移动端底部弹窗（bottom sheet）设计，改为侧边栏+主内容区并排布局

## v1.0.196
- 部署更新：项目构建与服务器部署

## v1.0.195
- 修复移动端键盘弹出时盖住输入框问题：visualViewport 动态设置 Layout 高度（键盘弹出时=可视区域高度，收起时恢复100dvh），100ms debounce防抖

## v1.0.194
- 修复移动端输入框获取焦点时页面放大问题：viewport 禁止缩放、输入框字号提升至16px（iOS Safari 阈值）、全局input/textarea最小字号16px、body添加-webkit-text-size-adjust:100%

## v1.0.193
- 所有57个角色模板提示词统一添加简洁回复要求（普通角色2-4句，律师/心理咨询师3-5句，英语外教2-4句英文）

## v1.0.192
- 新增20个怼人激怒系列角色模板（欠揍小助手、祖安文化大使、气人教练、阴阳怪气客服、揭短大师、抬杠运动员、嘴贱闺蜜、否定大师、反向心灵鸡汤、死对头、冷嘲热讽艺术家、阴阳怪气班主任、假温柔真扎心、卑微文学King、现实暴击师、摸鱼稽查员、气死人不偿命禅师、记仇小本本、卷王之王、普通但自信批判家）

## v1.0.191
- 将追问功能标记从【建议】统一改为【追问】：修改 suggestions.ts 正则/Prompt 和 MindmapChat.tsx 中的 Prompt 及注释

## v1.0.190
- 修复移动端点击输入框后页面崩溃看不到输入框的问题：去掉 visualViewport 对 #root 的 position:fixed+overflow:hidden 强制覆写，让浏览器 100dvh 原生处理键盘适配；输入框聚焦改用 scrollIntoView 确保可见

## v1.0.189
- 修复移动端键盘弹出时页面适配：visualViewport 改为直接操作 DOM（不经过 React state），避免重渲染延迟导致跳动；添加 debounce 防抖 + iOS offsetTop 修正 + -webkit-fill-available 兜底；输入框焦点增加 350ms 延迟等键盘弹完再定位

## v1.0.188
- 修复气泡 hover 闪动：去掉 scale 放大，hover 仅通过 translateZ 浮出球面 + 边框高亮，避免气泡放大后遮挡相邻气泡导致 hover 事件在气泡间来回跳

## v1.0.187
- 修复 3D 球体偶尔卡顿闪烁：旋转动画改用 ref + 直接 DOM 操作，绕过 React 渲染周期，不再每帧触发组件重渲染

## v1.0.186
- 3D 球体尺寸响应式优化：球体半径根据视口宽高动态计算，气泡大小随球体等比缩放，适配不同屏幕尺寸

## v1.0.185
- 移动端输入框优化：减小 padding、TextArea 从 2 行改为 1 行、点击输入框自动滚动到底部
- 修复 draggingRef 重复声明导致的构建失败

## v1.0.184
- 角色模板气泡改为 3D 球体展示：Fibonacci 球面分布 + CSS 3D Transform，鼠标拖拽旋转
- 移除 d3-force 力布局相关代码和依赖
- 空闲自动缓慢旋转，hover 气泡放大浮出，点击创建会话

## v1.0.183
- 思维导图默认关闭，所有设备均需手动打开
- 修复移动端输入框键盘弹出时页面不自适应问题：使用 visualViewport API + dvh 动态高度
- viewport meta 添加 viewport-fit=cover 和 interactive-widget=resizes-content

## v1.0.182
- 移动端优化：思维导图默认隐藏，通过聊天面板头部按钮可随时打开/关闭
- 移动端全屏导图模式新增关闭按钮，方便返回聊天界面
- 设备切换时自动调整思维导图可见性

## v1.0.181
- 后端强制关闭思考模式：enableThinking 硬编码 false，reasoningEffort 清空，所有分支加 enableThinking 守卫，彻底杜绝 reasoning_content 返回

## v1.0.180
- 追问提示词优化：追问站在用户角度，"建议问题"改为"追问问题"
- 前端流式渲染优化：引入 flushSync 实现 AI 回复逐字流式显示
- 37个角色模板新增开场白（greeting 字段）

## v1.0.179
- 系统移除思考过程：前端不再展示"💭 思考过程"区域，模型配置面板和会话设置中不再提供思考模式/推理力度开关，后端强制 enableThinking=false

## v1.0.178
- 圆圈初始位置和大小改用 Math.random()，每次刷新页面都重新随机布局

## v1.0.177
- 欢迎页角色圆圈进一步调整：移除向心力、增加硬边界和标题区硬排除，让圆圈铺满整页而不挡住欢迎语标题
- 圆圈内文字按字符数自动换行并动态计算字号，确保文字填满圆圈、不再省略

## v1.0.176
- 修复遗漏 useMemo 导入导致的运行时崩溃（ReferenceError: useMemo is not defined）

## v1.0.175
- 角色圆圈重磅改版：引入 d3-force 力导向物理仿真引擎，圆圈大小不一（56-100px）、自然散布无规律、碰撞推开无重叠；名字放入圆圈内，去掉机器人图标；hover 时暂停浮动动画并放大

## v1.0.174
- 角色圆圈改用网格布局：8x7 网格 + 避开中心标题区 + 格子内小幅抖动，彻底解决圆圈和标签重叠问题

## v1.0.173
- 角色圆圈碰撞避免：使用螺旋搜索算法确保各圆圈互不重叠，仍然保持确定性分布和浮动动画

## v1.0.172
- 角色圆圈改为绝对定位随机散布在欢迎页面上，避开中间标题区域，带毛玻璃效果和浮动动画

## v1.0.171
- 首页角色圆圈优化：去掉渐变背景色改为白色半透明、乱序展示、添加浮动动画（上下浮动 + 交错延迟）

## v1.0.170
- mock 20 个默认角色模板写入数据库：东北人、台湾人、上海人、律师、女朋友、男朋友、好兄弟、陪伴达人、毒舌吐槽、夸夸机器人、赛博算命师、霸道总裁、心理咨询师、英语外教、喵星人、历史人物、深夜电台、废柴人生导师、北京大爷、广东人，每个角色含详细 System Prompt、分组、温度等配置

## v1.0.169
- 首页新增默认角色圆圈展示：管理员可后台配置角色模板（名称、标题、背景色、分组、会话级模型配置），所有用户在首页可通过点击角色圆圈一键创建预设配置的对话
- 后端：新增 `role_templates` 表（init-db 建表 + db.js 幂等迁移），新增 GET/POST/PUT/DELETE `/api/role-templates` 与 `/api/role-templates/all` 接口，新增 `POST /api/conversations/from-role-template` 基于模板创建会话
- 前端：首页欢迎页新增角色圆圈区域（名字浮动在圆圈上方、渐变色背景、hover 动画），侧边栏管理员入口「角色管理」弹窗（新建/编辑/删除/启用切换）

## v1.0.168
- 新增 docs/architecture-diagrams.md，包含业务架构图、用例图、时序图（登录、对话、追问、同步）、技术架构图、数据模型 ER 图

## v1.0.167
- 暂时屏蔽图片功能入口（上传按钮、粘贴/拖入图片、图片预览），待底层大模型支持多模态后再开启

## v1.0.166
- 供应商切换：选择供应商时自动填入对应的 API Key、Base URL、Region、Model、WorkspaceId，无需手动填写

## v1.0.165
- 模型选择：所有模型 label 添加平台标识（[腾讯] / [阿里]），方便区分来源

## v1.0.164
- 模型配置：腾讯云供应商补充 DeepSeek-V4-Pro-202606、DeepSeek-V4-Flash-202605 两个带日期版本模型

## v1.0.163
- 模型配置：阿里云供应商新增截图中的 12 个模型（Qwen3.7 系列、Qwen3.5 系列、DeepSeek-V4-Pro、GLM-5.2、Kimi K2.7 Code、Kimi K2.6 等），默认模型同步更新为 qwen3.7-max

## v1.0.162
- 修复图片未发送给大模型的问题：embedImagesInMessages 文件名提取兼容绝对 URL 格式

## v1.0.161
- 修复上传图片后输入框无缩略图问题：onImagesChange 在 activePanelId 为空时回退使用 activePanel.id
- 欢迎页 ChatInput 新增 images/onImagesChange 支持，新增 welcomeImages 状态管理上传图片

## v1.0.160
- 修复图片上传返回路径为绝对 URL（http://&lt;server-ip&gt;:11113/uploads/xxx），支持 BASE_URL 环境变量

## v1.0.159
- 修复纯图片消息无法发送的问题：handlePanelSend 允许无文本仅图片时发送，纯图片节点标题显示为「[图片]」

## v1.0.158
- 部署多模态能力支持到生产环境，确认图片上传、Vision 格式转发、视觉模型配置全链路可用

## v1.0.157
- 多模态能力支持：ChatInput 支持粘贴/拖拽/点击上传图片，图片存储到服务器文件系统（`/api/upload`）
- chat-proxy 代理层自动将图片转 base64 嵌入 OpenAI Vision 格式（content 数组），兼容阿里云百炼和腾讯混元
- 数据库 messages 表新增 images 字段，视觉模型列表新增 qwen-vl-plus/max、glm-5v-turbo

## v1.0.156
- 修复首页欢迎页不显示的问题：未选中会话时展示「思维导图聊天」欢迎页，仅选中空会话时展示「有什么我能帮你的吗？」提示

## v1.0.155
- 全站操作日志系统：后端 `operation_logs` 表存储服务端操作日志（登录/注册/会话CRUD/消息/模型配置/AI请求），`GET /api/admin/logs` 供管理员查询
- 前端统一日志工具 `src/utils/logger.ts`：结构化输出到 console（按 auth/chat/conversation/settings/model_config/admin/learn/ui 分类着色），覆盖所有用户行为

## v1.0.154
- 点击 MindmapChat 品牌区不再跳转第一个会话，改为返回欢迎页；页面初始加载默认展示欢迎页，会话侧边栏保留

## v1.0.153
- 移除不再使用的 DoubaoChatTree 旧版对话树页面及相关组件（ChatNode、MeNode、Toolbar、SuggestionChipNode、layout.ts），删除 App.tsx 中 /chat 和 /thinkchat 路由
- 清理 index.css 中 react-flow 和 ChatNode 相关死代码样式

## v1.0.152
- 修复发送消息时自动退出登录的问题：chat-proxy 透传上游 AI 服务的 401（API Key 无效时），原代码不区分响应来源直接 doLogout + 跳转登录页；现在区分认证层 401 和上游 AI 401，上游 API Key 错误仅显示错误提示，不再强制退出

## v1.0.151
- 模型配置保存按钮增加 loading 状态，等待服务端响应后才关闭弹窗，确保用户能看到保存结果提示
- 优化保存失败时的错误提示：401 未登录/过期时明确提示「登录已过期，请重新登录」，其他错误展示服务端返回的具体错误信息

## v1.0.150
- 模型配置弹窗：切换服务商时模型字段自动切换为下拉选择，按厂商分组展示该服务商支持的模型列表（腾讯 TokenHub 含混元/DeepSeek/GLM/Kimi/MiniMax/Qwen 六大系列 27 个模型，阿里云百炼含 Qwen 系列）；模型下拉支持关键字搜索
- 腾讯云服务商开放「联网搜索」开关（之前强制隐藏，TokenHub 平台支持该参数）

## v1.0.149
- 会话列表分组功能（conv_group 字段 + 侧边栏分组展示）、会话置顶（is_pinned 字段 + 置顶图标按钮，排序置顶优先）
- 品牌名 "Mindmap Chat" → "MindmapChat"；点击品牌文字/图标整页刷新回到欢迎页；发送消息框支持垂直拖拽缩放

## v1.0.148
- 会话级「推理强度」下拉按服务商动态切换选项：复用 `ModelConfigModal` 的 `reasoningOptions(provider)`，阿里云显示 high/max、腾讯显示 high/low；`convReasoning` 类型扩为 `'high' | 'max' | 'low'`，`effReasoning` 解析改为「会话级有值即生效」，使腾讯会话级可设 low；`ModelConfigModal` 导出 `Provider` 类型与 `reasoningOptions` 函数供两页复用

## v1.0.147
- 前端流式渲染腾讯混元思考过程（reasoning_content）：① 两页 `fetch('/api/chat')` 流式解析 `delta.reasoning_content` 并实时写入消息；② assistant 气泡在正文前以「💭 思考过程」折叠块（`<details>`，默认关闭）渲染，思考模式默认关闭故默认不展示；③ 持久化：`messages` 表新增 `reasoning` 列（幂等迁移 + init-db 建表），`getMessages`/`createMessage`/`updateMessageContent` 与 `/api/messages`、`/sync` 全链路透传，重载会话后保留

## v1.0.146
- 新增腾讯混元大模型（TokenHub，hy3）支持：① 后端代理 `aliyun-chat-proxy.js` 泛化为多服务商 `chat-proxy.js`（同步更新 `server.js`/`server-prod.js`/`init-db.js`/`db.js` 的 import），按 `provider` 字段切换 endpoint 与思考参数映射——阿里云走 `extra_body.enable_thinking`+`reasoning_effort`(high/max)，腾讯走顶层 `thinking:{type:"enabled"}`+`reasoning_effort`(high/low) 且不支持 `enable_search`；② `global_config`/`user_configs` 增加 `provider` 列（幂等迁移）并读写透传；③ 前端 `ModelConfigModal` 新增「服务商」选择，选腾讯时隐藏 Workspace ID、Region 改为 gz/sg 下拉、Base URL 默认腾讯地址、模型默认 `hy3`、推理力度改为 high/low、隐藏联网搜索；④ 两处 `fetch('/api/chat')` 请求体补充 `provider: mc.provider`

## v1.0.145
- 修复安全 bug：`/api/admin/users` 与 `/api/admin/users/:userId/questions` 补上 `requireAdmin` 中间件，非管理员不再可读全站用户列表与提问；`server-prod.js` 启动期与 `init-db.js` 建表均补 `users.nickname` 列（兼容旧库，线上已存在则跳过）

## v1.0.144
- MindmapChat 新增「自定义追问」：在 assistant 消息的系统建议之外，用户可点击虚线「✏️ 自定义追问」输入自己的问题并追问，复用现有派生逻辑（独立子容器 + 立即发送），发出后显示为「已追问」标签可点开分支；locked 时隐藏入口

## v1.0.142
- 修复 MindmapChat 切到含消息的会话时 React #300 崩溃：根因是 MessagePanel 的 useMemo(panelMap) 写在 `if (!panel) return` 提前返回之后，违反 Hooks 规则，panel 在 null/非null 间切换时 hook 数量不一致。将 useMemo 移到所有提前 return 之前

## v1.0.141
- 修复 MindmapChat 切换回「空壳会话」(服务器 panels 为空) 后无法发送：① handleSwitchConversation 的 /full 返回空 panels 时自动补一个默认容器面板并设为 activePanelId；② 欢迎页 onSend 兜底：会话存在但无面板时即时补容器面板并复用当前会话发送，不再误提示"会话加载中"卡死

## v1.0.136
- 移除 pollNow 轮询、版本号(version)比对、编辑锁(lock) 整套多页面同步机制：① 前端删除 pollNow、定时轮询、BroadcastChannel 跨标签同步、冲突提示条、同步状态按钮及 lastSavedVersions/dirtyConvs/pendingVersion 等状态，scheduleSync 仅保留防抖持久化；② 后端删除 /api/conversations/versions 与 /lock、/lock/renew、/lock/release 接口及 db.js 中 acquireLock/renewLock/releaseLock，保留 bumpConversationVersion 以维持会话排序。多标签页实时同步失效（需刷新页面查看其它设备更新），换取新建会话发送 100% 稳定

## v1.0.135
- 彻底修复新建会话无法发送：根因是 pollNow 轮询用数据库 panels 覆盖了内存中刚建好、含 panelId 的会话（若 apiPost 保存 panel 失败则库里无 panel，覆盖后 panelId 丢失，发送必失败）。两处加固：① pollNow 拉取时若本地会话已有面板则保留本地 panels（不再清空 panelId）；② handlePanelSend 定位会话改为 activeIdRef 优先（新建后 setActiveId 一定已执行，最可靠），panelId 反查作兜底

## v1.0.134
- 修复新对话发送「完全没反应」：根因是创建会话时把 panels:[] 存入数据库，轮询同步(pollNow)从库加载回来用空 panels 覆盖了内存中刚建好、含 panelId 的会话，导致 handlePanelSend 用 panelId 反查找不到会话；且上一版把 throw 放在 try 块外，异常无法被 catch 捕获 → 输入框不清空、无 loading、无报错。改为：① 创建会话时传真实 panels（pollNow 加载回来也含 panelId）；② 找不到会话的 throw 移回 try 内并增加 activeIdRef 兜底，任何找不到都显示错误而非静默失败

## v1.0.133
- 修复新对话发送无反应：欢迎页「建会话 + 100ms 后发送」场景下，handlePanelSend 依赖闭包里过期的 activeId 定位会话，导致找不到新会话（或发到错误旧会话）。改为用 panelId 反查所属会话，彻底摆脱 activeId 闭包依赖

## v1.0.132
- 修复「保存模型配置不生效」根因：后端代理 aliyun-chat-proxy.js 原先完全忽略前端传来的 model/apiKey/region 等，只用自己的数据库全局配置或 defaultModel，导致页面保存后实际请求仍用旧模型。改为优先采用客户端传入值、数据库全局配置兜底
- 模型配置保存增加成功/失败提示（不再静默失败），失败也先本地保存

## v1.0.131
- 修复模型配置保存后不生效：发送请求时 model 字段被硬编码为 'qwen3.7-max-2026-05-17'，未使用用户配置；MindmapChat 与 DoubaoChatTree 两处均改为 mc.model
- 模型配置保存时同步写入 localStorage，避免刷新后回退；日志模型名改为实际值

## v1.0.130
- 输入框支持手动调整大小：移除 autoSize 自动撑高，改为原生垂直拖拽手柄（resize: vertical，最小高度 40px），可拖动右下角自由调整高度

## v1.0.129
- 优化导出 PDF 打印样式：标题区带对话名 + 条数/导出时间分隔线；消息改为卡片式气泡（用户右对齐浅紫、AI 左对齐浅灰带边框），角色标签配色区分；强制保留代码块/引用/表格配色

## v1.0.128
- 导出 PDF 改用浏览器原生 window.print() + 打印样式方案，彻底移除 html2pdf.js/html2canvas 截图（原截图方式在当前环境始终生成空白 PDF）；打印时仅显示选中消息区，用户在打印对话框选「另存为 PDF」即可下载，Markdown 与代码高亮由浏览器真实渲染，保证有数据

## v1.0.127
- 修复导出 PDF 仍空白：根因为选中 key 用 `${面板id}-${消息id}` 与导出遍历 allPanels 的 key 跨面板不匹配，改为以消息 id 作 key；导出时遍历 allPanels 按原顺序过滤并渲染真实内容
- 加固导出：临时容器挂到 document.body 视口内可见、等待字体与布局完成后再截图，避免 html2canvas 截到空白

## v1.0.126
- 修复对话导出 PDF 空白：导出改为克隆内容临时挂到 document.body 视口内截图，并重置离屏容器定位样式（避免被祖先裁剪）
- 多选/导出工具条由面板顶部移至底部（消息区下方），固定不随消息滚动

## v1.0.125
- 新增思维导图页(/)对话导出 PDF：消息区顶部「多选」开关，勾选若干条消息（可跨节点累积），「导出选中」按原顺序生成 `对话名-选中N条.pdf`（AI 回复保留 Markdown 格式，含代码高亮）；新增 html2pdf.js 依赖与 src/utils/exportPdf.ts 工具
- 整理部署文件：移除过期打包产物与运行日志，concurrently 由 dependencies 移至 devDependencies

## v1.0.120
- 修复：内容区顶部标题栏（🔗 来源路径 + 会话名）在选中无容器祖先的消息节点时整条消失的问题；现至少显示会话名（默认名如"新角色"），消息节点有父级时一并显示父节点名

## v1.0.119
- 修复：用户发送消息时不再自动将会话名称改为首条消息文本；会话名保持为默认（新角色/新对话）或用户在「对话设置」中手动设定的名称

## v1.0.118
- 新增会话级模型配置：每个会话可单独设置 System 提示词、温度（Temperature）、思考模式（Thinking）、推理强度（Reasoning Effort），仅对该会话生效；未设置时回退全局「模型配置」
- 后端：conversations 表新增 conv_system / conv_temperature / conv_thinking / conv_reasoning 四列（幂等迁移）；PUT /api/conversations/:id 与 /full 接口支持读写；chat 代理按会话级参数覆盖全局配置
- 前端：MindmapChat 与 DoubaoChatTree 的「对话设置」弹窗新增「会话级模型配置」区块（仅管理员可见），保存后经 /api/chat 生效

## v1.0.116
- 新增权限系统：仅管理员（is_admin）可配置大模型、apiKey 等，配置写入 global_config 表后全局生效；普通用户只读，聊天强制使用管理员统一设定的连接配置
- 后端：users 表新增 is_admin 字段并支持配置管理员账号；新增 global_config 表与 GET/PUT /api/config/global 接口（写入需 requireAdmin）；chat 代理的 apiKey/region/workspaceId/model/baseUrl 始终以服务端全局配置为准，忽略客户端传入
- 前端：模型配置入口与会话级模型配置区块仅对管理员显示；普通用户保存对话设置不再写入模型参数；登录持久化 isAdmin

## v1.0.115
- 移除「当前对话已在其他页面更新，是否采用最新数据？」冲突确认弹窗（采用远程/保留本地）
- 改为定时自动同步：轮询发现远程版本更新时，若本地无进行中编辑/未脏则自动采用最新数据；若本地有未保存改动或正在流式输出，则跳过本次、下次轮询再尝试，不再打断用户
- 编辑锁窗口仅依赖定时从服务器同步（移除 data_version 冲突横幅，与锁的软互斥分工明确：锁管「谁能改」，定时同步管「改完自动拿到最新」）

## v1.0.114
- 新增会话编辑锁（多页面互斥编辑），防止多个页面同时打开同一会话时乱修改
- 后端：conversations 表新增 lock_owner / lock_label / lock_expires_at 三列；新增原子抢锁 POST /api/conversations/:id/lock、续租 /lock/renew、释放 /lock/release 三个接口；/versions 与 /full 返回锁信息
- 前端：每个标签页生成稳定 clientId（sessionStorage）与可读标识（浏览器名+短id）；发送/重新生成/追问/重发/重命名/删除等编辑操作前先抢锁，被他人持有时放弃并提示「该会话正由「<对方标识>」编辑中（约剩 Xs）」
- 锁时间设计：租约 TTL=60s；持锁期间每 15s 续租（=TTL/4，留 4 次容错）；空闲超过 60s 自动释放；页面关闭/切换会话时立即释放（beforeunload + keepalive）；后台标签页不释放（靠 TTL 兜底）；不提供强制接管
- 锁为软互斥，与既有 data_version 冲突横幅互补：锁管「谁能改」，版本号管「改完他人如何拿到最新」

## v1.0.111
- 来源路径（主对话框上方）的根节点改用会话名字显示：根容器（parentId 为 null）原本显示默认标题"新角色"，现改为显示所属会话的 Conversation.title；其余容器节点仍显示各自标题

## v1.0.110
- 新增多页面数据同步功能：基于 data_version 版本号 + 定时轮询 + BroadcastChannel 跨标签页通知，多个页面打开同一会话时可近实时同步最新数据
- 后端新增轻量版本查询接口 /api/conversations/versions，/full 与 /sync 接口返回并递增 dataVersion；数据库 conversations 表新增 data_version 字段
- 前端新增冲突处理（本地脏标记/流式输出保护、冲突横幅可选采用远程或保留本地），设置面板新增"数据同步（多页面）"开关与轮询间隔（3/5/8/15/30 秒，默认 8 秒）

## v1.0.108
- 将默认大模型从 qwen3.7-max-2026-06-08 切换为 qwen3.7-max-2026-05-17（前端调用、ModelConfigModal 默认值、server/server-prod/aliyun-chat-proxy 默认模型同步更新）

## v1.0.107
- 主对话框打开/切换/跳转时，最后一条用户消息改为顶部对齐（block: start）而非居中，直接放在头部

## v1.0.106
- 优化主对话框滚动定位：① 新增 selectPanel 封装，每次显式选择（含重复选中同一面板）自增 nonce 强制重新定位；② 用 requestAnimationFrame 等异步内容（代码块/图片）布局稳定后再定位，避免偏差；③ 滚动改为平滑（behavior: smooth）

## v1.0.105
- 来源路径（主对话框标题上方）仅串联容器节点：向上回溯时穿透消息节点，只收集 type=container 的祖先；当前面板为容器时作为末项高亮，为消息节点时只展示其容器祖先，不再把消息节点纳入链路

## v1.0.104
- 打开/切换对话框时，焦点定位到最后一条用户消息位置（居中），不再自动拉到最底部；发送消息（加载中）时仍跟随流式回复滚动到底部

## v1.0.103
- 去掉主对话框标题栏（蓝色图标+标题行），仅保留上方的来源路径作为当前节点标识

## v1.0.102
- 将思维导图容器节点的来源路径从面板内部移到主对话框标题上方（🔶 前缀 + › 分隔），点击祖先节点可切换主对话框到对应面板；思维导图小面板不再重复显示该链路

## v1.0.100
- 真正实现 v1.0.92 来源链路栏：原实现位于未被引用的 ChatNode.tsx（React Flow 节点，页面未使用），实际思维导图由 MindmapChat 自定义渲染。现改为在 MindmapChat 容器节点 Header 下方新增来源链路栏（🔗 前缀 + › 分隔，展示根到当前节点的祖先路径），点击链路节点平滑居中跳转

## v1.0.99
- 调整收起侧边栏宽度：由 48px 收窄为 32px，恰好容纳「展开侧边栏」按钮且不再溢出

## v1.0.97
- 修复 DoubaoChatTree 侧边栏收起后无法展开：收起时保留 48px 边栏并显示「展开侧边栏」按钮（MenuUnfoldOutlined），点击恢复，与 MindmapChat 行为一致

## v1.0.94
- 调整侧边栏展开入口：取消页面内浮动按钮，改为收起时在最左侧保留 48px 边栏并显示「展开侧边栏」按钮，点击恢复

## v1.0.93
- 修复侧边栏收起后无法打开：收起时在主内容区左上角显示「展开侧边栏」浮动按钮（MenuUnfoldOutlined），点击恢复

## v1.0.92
- 对话容器节点 Header 下方新增来源链路栏：展示从根节点到当前节点的祖先路径（🔗 前缀 + › 分隔），点击链路节点可平滑居中跳转至对应对话容器

## v1.0.89
- 消息支持重新发送与 AI 响应重新生成：抽出核心流式函数 runChatStream 复用；新增 handleRegenerate（替换旧 AI 回复，上下文剔除旧回复）/ handleResend（重发原文）；消息气泡下方加「重发」「重新生成」按钮（仅容器节点）

## v1.0.91
- 对话列表项移除直接删除图标，删除入口收进「对话设置」弹窗（带 Popconfirm 二次确认）；每个对话新增设置按钮，可修改对话名称与对话背景（预设色板 / 自定义颜色 / 背景图 URL）
- 后端：conversations 表新增 background 列（启动时幂等 ALTER 兼容旧库），GET/POST/PUT 会话接口支持 background 持久化；内容区背景动态应用对话背景

## v1.0.88
- 调整 MindmapChat 的 MessagePanel 头部（思维导图左侧面板）：高度收窄（padding 由 10px 减到 6px，底边框由 2px 减到 1px），移除「容器/消息」标签，移除右上角 X 关闭按钮

## v1.0.87
- 删除对话页（MindmapChat / DoubaoChatTree）顶部 header 模块（仅删除，不迁移按钮）；消息区恢复原始布局，顶部不再保留 header 高度

## v1.0.84
- 消息复制功能调整：只复制消息纯文本（不再复制 JSON），复制按钮从气泡右上角移到消息体下方（用户消息右对齐、助手消息左对齐）

## v1.0.82
- 历史对话列表新增删除按钮：MindmapChat 桌面侧边栏与移动抽屉、DoubaoChatTree 移动抽屉均补齐（调用 DELETE /api/conversations/:id，删除后自动切换/清空当前会话）

## v1.0.80
- 聊天请求模型写死为 qwen3.7-max-2026-06-08（DoubaoChatTree / MindmapChat 两处 fetch 不再从配置读取 model）；loadConfig 恢复读取用户配置（apiKey、temperature 等仍生效）

## v1.0.78
- 前端模型配置强制锁定为 qwen3.7-max-2026-06-08：loadConfig 忽略已保存的 model 字段，始终使用默认模型（其余设置仍保留用户已存值）

## v1.0.76
- 大模型默认模型切换为 qwen3.7-max-2026-06-08（前端默认配置 + 服务端/代理兜底 defaultModel 同步更新）

## v1.0.74
- 新增 /learnwords 页面：AI 讲单词 Agent 纯前端 UI/动效演示（首页→识别处理→单词确认→单词卡[默写/选词/配对]→文章[翻译/填空]→学习报告→循环强化），暂未接入后端交互

## v1.0.73
- 每个消息框（用户/AI）右下角新增复制按钮，点击复制该条消息的完整数据（id/role/content/suggestions/parentMessageId）为 JSON，并提示成功/失败

## v1.0.71
- 建议生成健壮性增强：解析兼容【建议】前后冒号/星号、无序列表(-/*)、[1] 及同行首条等多种格式；新增兜底机制——主回复未解析到建议时自动用一次轻量请求补生成，确保每条回复都有建议

## v1.0.69
- AI 回复末尾建议升级：先总结关键点，再生成 1-5 条后续问题建议（原固定 3 条），解析上限同步放开到 5 条

## v1.0.67
- 登录页副标题改为「把对话变成思维导图，让每一次追问都有迹可循」

## v1.0.65
- 界面品牌名由 ThinkChat 统一改为 Mindmap Chat（登录页、注册页、对话页标题、工具栏品牌名、默认模型名）

## v1.0.63
- 移除移动端底部弹窗（面板详情）头部的 × 关闭按钮

## v1.0.62
- 移除思维导图侧边栏（桌面端 + 移动端抽屉）中的「← 回到对话视图」按钮

## v1.0.61
- 移除「新角色」按钮左侧的 MessageOutlined 图标（桌面端 + 移动端抽屉）
- 移除首页全部建议标签（资讯、Redis、制冷系统），MindmapChat 与 DoubaoChatTree 两处 suggestionTags 置空

## v1.0.60
- 修复提问历史堆栈递进：补全消息 parent_message_id（assistant 指向触发的 user 消息、user 指向回复的 assistant），使消息链连续；历史数据幂等迁移补全，/user-questions 严格按堆栈结构缩进递进

## v1.0.59
- 用户提问页(/user-questions)严格按消息堆栈结构打印：后端返回全部消息及 parent_message_id/sort_order；前端按 parentMessageId 构建消息树，前序 DFS 得到严格顺序，并按提问轮次层级缩进（替代原先按数组索引平铺）

## v1.0.58
- 移除会话列表（桌面端 + 移动端抽屉）中每项的「删除会话」按钮，避免误删对话；同步清理 handleDeleteConversation 函数与 DeleteOutlined 导入

## v1.0.57
- 收紧思维导图「追问」标签判定：仅在「消息节点(message) → 追问容器(container，带 sourceSuggestionIndex)」这条连线上显示，普通容器里发消息（容器 → 消息节点）不再误标

## v1.0.56
- 思维导图连线标注：点击追问时，消息节点 → 追问容器的连线上显示浅紫色「追问」文字标签（仅追问派生线显示，普通发消息连线不显示），随视图缩放自适应

## v1.0.55
- 修复思维导图节点类型：为 panels 表新增 type 字段（容器/消息节点），加载会话时按 type 渲染；消息节点（仅含一条用户消息）正确渲染为消息节点而非容器节点，并修正历史数据

## v1.0.54
- 思维导图自动适配视图：调整对话框宽度（拖拽分隔条时实时）与添加子节点（追问派生容器 / 直接发消息生成消息子节点）后，自动触发"适应视图"，确保整张思维导图始终完整可见

## v1.0.53
- 容器状态与追问交互优化：
  1. 每个容器新增状态字段 status（1=正在思考，2=已完成）；AI 输出完成后容器状态更新为 2
  2. 点击追问时子容器状态置为 1（追问中，按钮转圈展示 "追问中"），当容器状态变为 2 后按钮更新为 "已追问" 并可点击进入
  3. 页面首次进入对话/导图视图时，对话框占 60%、思维导图占 40%

## v1.0.52
- 移动端兼容（手机端可用）：
  1. 补全移动端抽屉菜单：新增历史对话列表（可切换/删除）、用户问答入口、模型配置、退出登录、用户信息，此前手机端无法切换对话/配置/退出
  2. 底部对话面板改为可折叠 bottom sheet：顶部把手条点击折叠/展开，关闭按钮彻底收起，默认折叠避免遮挡思维导图
  3. 点击思维导图节点自动展开对话面板；新建/切换对话后默认折叠面板；发送消息后自动展开以显示回复
  4. 修复手机端误触"隐藏思维导图"后无法恢复的问题（"显示思维导图"按钮在手机端不再被禁用）
  5. 移动端进入思维导图时自动适配视图（fit），避免节点超出屏幕

## v1.0.51
- UI 文案与交互调整：
  1. "新对话" 改为 "新角色"（按钮及新建默认标题）
  2. 浏览器标签页标题改为 Mindmap Chat，侧边栏标题统一为 Mindmap Chat
  3. 左侧对话列表图标改为小人图标（UserOutlined）
  4. 建议按钮文案由 "+ xxx" 改为 "追问：xxx"
  5. "已派生" 改为 "已追问"
  6. 追问生成中子容器回复未完成时，对应建议按钮转圈，完成后显示"已追问"

## v1.0.50
- 将思维导图页设为默认首页：/ 现为思维导图视图，原对话视图移至 /chat，/mindmap 重定向到 /（兼容旧链接），/thinkchat 重定向到 /chat

## v1.0.49
- 修复一次性追问多个建议导致消息错乱：
  1. buildMessagesPath 的 fork 截断逻辑：被追问的答案（fork 消息之后的 assistant 回复）现在会正确纳入上下文，不再"答非所问"
  2. handleDerivedClick 的已派生判断移入 setConversations functional update，避免并发点击 / ref 滞后导致重复创建与重复发送

## v1.0.48
- 修复追问重复创建子容器：渲染层正确识别已派生子容器（扫描全部面板），并在 handleDerivedClick 增加已存在判断守卫

## v1.0.47
- 还原追问按钮逻辑：未派生的保持原逻辑（立即创建子容器并发送），但不自动打开派生的容器
- 已派生的追问直接点击打开对应容器节点
- 思维导图消息节点：超过 30 字自动省略（..），并加上浅灰底/边框/圆角基础样式

## v1.0.46
- 追问按钮：点击后转圈并禁用，待 AI 结果返回后再渲染子节点；已派生的追问不可再点
- 思维导图消息节点：超过 30 字自动省略（..），并加上浅灰底/边框/圆角基础样式

## v1.0.44
- 思维导图（/mindmap）改为由 React SPA 路由提供，移除指向独立 mindmap-demo.html 的静态路由
- 消息节点改为纯文本展示，去除卡片样式
- 对话框与思维导图支持拖拽调整大小、相互切换、全屏适配与视图缩放

## v1.0.40
- 默认模型改为 `qwen3.7-max-2026-06-08`
- 模型配置弹窗：API Key、地域、WorkspaceId、BaseURL 隐藏到"高级连接配置"折叠面板中，默认收起

## v1.0.38
- **移动端/平板适配**：
  - 新增 `useResponsive` hook，支持 mobile(≤640px) / tablet(641-1024px) / desktop(>1024px) 断点
  - `index.css` 新增全局 `@media` 规则，自动限制 Modal/Drawer 最大宽度
  - `LoginPage` / `RegisterPage` 卡片改用 `maxWidth` + `clamp()` 自适应
  - `ModelConfigModal` 宽度响应式 `min(720px, calc(100vw - 32px))`
  - `SettingsPanel` Drawer 宽度响应式
  - `DoubaoChatTree` 主页面：移动端侧边栏自动隐藏，通过汉堡菜单唤起 Drawer；平板端侧边栏缩窄为 240px；欢迎页/输入区自适应缩放
  - `UserQuestionsPage` 用户问答页：移动端改为上下布局，选中用户后显示返回按钮

## v1.0.36
- 移除模型配置中的“联网搜索”开关

## v1.0.34
- “用户问答”菜单和页面权限控制：仅管理员可见

## v1.0.33
- 新增“用户问答”页面：左侧用户列表，右侧堆栈式展示选中用户的会话和提问历史
- 后端新增 `GET /api/admin/users` 和 `GET /api/admin/users/:userId/questions` 接口
- 侧边栏新增“用户问答”导航入口

## v1.0.30
- Markdown 字体再缩小：12px 基础字号，全面下调各级标题字号
- line-height 再收紧：1.45 → 1.35，blockquote、列表、代码块同步
- AI 输出气泡移除 `white-space: pre-wrap`，减少残留换行，padding 缩小

## v1.0.29
- Markdown 全局字号缩小：容器 13px，标题、段落、列表等同步下调
- 行间距全面收紧：line-height 统一降至 1.45，margin/padding 缩小

## v1.0.28
- skill 新增部署前更新 CHANGELOG.md 步骤

## v1.0.27
- 方案 A：预处理层新增 `\n{2}` → `\n`，彻底干掉段落分隔产生的多余空行
- 方案 C：`.markdown-content p` margin 从 2px 降为 0，配合 line-height 保持可读性

## v1.0.24
- 修复切换会话时 panels 为空导致白屏崩溃的竞态条件
- 添加 panels[0] 空数组防御

## v1.0.20
- AI 思考指示器改为旋转圆环动画（替换闪烁竖线）

## v1.0.19
- 移除 Markdown 格式提示词，还原大模型原生输出
- 前端框架层面压缩 Markdown 显示：连续多空行合并为单行，列表项间不留空行，段落间保持紧凑间距
- MarkdownRenderer 增加预处理：`\n{3,}` 压缩为 `\n\n`，列表项间空行自动移除

## v1.0.18
- 优化 Markdown 渲染提示词：段落间保留一行空行，列表项间不留空行，禁止多余 `<br/>`

## v1.0.16
- 精简 Markdown 渲染提示词：只限制列表项间距，其余保持原生格式

## v1.0.14
- 从 system 提示词层面控制 Markdown 输出紧凑度，减少多余换行

## v1.0.12
- 收紧 Markdown 样式间距：h1-h6、p、ul/ol、blockquote、pre 等元素 margin 大幅缩小
- MarkdownRenderer 跳过空白段落，避免多余换行

## v1.0.10
- 修复 `/api/chat` 401 时错误信息丢失问题，增加统一 `handleAuthError` 处理
- 页面新增退出登录功能（Header 右侧 + 欢迎页右上角）
- 登录页新增注册按钮
- 部署脚本补充缺失的 `db.js`、`init-db.js` 文件
